# origin: STE-v4（2026-07-06 復活）
# synthesis/ — Intake Adapter の中間生成物

- `session_digests/{date}_{slug}.md` — raw を整理したダイジェスト（読み物）
- `update_proposals/{date}_{target}_proposal.md` — bible への反映指示書。**G-Intake で人間が承認する対象**

生成は AUTO（あくまで「案」）。反映は human approval 必須（`adapter/human_approval_policy.md`）。
