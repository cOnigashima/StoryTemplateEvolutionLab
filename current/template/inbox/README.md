# origin: STE-v4（2026-07-06 復活）
# inbox/ — raw 受付箱

企画チャットのログ・断片メモ・外部生成 bible パッケージを**加工せずそのまま**置く場所。

- `planning_sessions/{YYYY-MM-DD}_{slug}.md` — 企画チャット / 既存 bible package
- `fragments/` — 断片メモ

**raw はここで止まる**。bible へは `synthesis/update_proposals/` + G-Intake 承認を経由（`core/rules/intake-flow.md`）。
